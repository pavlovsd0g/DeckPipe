# DeckPipe для Windows

Синхронизация Deezer и SoundCloud с локальной музыкальной библиотекой. Текущий объём релиза — только Windows x64. macOS исключена из текущей разработки и критериев приёмки.

## Как организована музыка

1. Сначала пользователь явно выбирает **одну общую папку со всей музыкой**. Без этого синхронизация недоступна. Папка по умолчанию автоматически не назначается.
2. Приложение индексирует вложенные папки в постоянном каталоге SQLite. Уже скачанный трек учитывается в любом плейлисте стриминга, включая «Лайки»; рядом виден фактический путь.
3. Папку назначения можно отдельно привязать к плейлисту, даже если названия различаются. Явно привязанные внешние папки также входят в каталог. Привязка определяет место для новых загрузок.
4. Скачиваются только отсутствующие треки. Совпадение проверяется по идентификатору сервиса либо исполнителю, полному названию версии и длительности. Несколько кандидатов требуют выбора. Существующие файлы не перемещаются ради структуры стриминга.

При недоступном носителе синхронизация останавливается. Пересканирование обнаруживает перемещения, удаления и замену файлов. Каталог хранится в профиле приложения отдельно от музыки; `.deckpipe.json` остаётся состоянием операций в каждой папке.

## Подключение сервисов

DeckPipe открывает отдельное окно входа для Deezer или SoundCloud и хранит собственную сессию в защищённом профиле Windows. Первая авторизация не использует сессию обычного браузера. Кнопка «Выйти и забыть вход» удаляет сохранённую авторизацию выбранного сервиса; «Сменить аккаунт» сначала завершает её, затем открывает новое окно входа. В интерфейс не возвращаются пароли, cookies и токены.

SoundCloud сохраняет существующие `yt-dlp`/api-v2 механизмы чтения источников, лайков и загрузки. Создание плейлистов SoundCloud и одинаковый набор операций с Deezer не являются условием текущего релиза. Неполная коллекция или отказ сервиса показываются явно. Неудавшееся добавление в Deezer хранится отдельно от локальной загрузки и повторяется с проверкой содержимого плейлиста.

## Состояние разработки

Актуальные требования и условия выпуска: [план релиза](docs/release-plan-2026-09-09.md). Подробная реализация A/B: [инженерный план](docs/superpowers/plans/2026-09-09-windows-stages-ab.md).

Синхронизация с Rekordbox и переключение путей на WAV реализуются в [этапе C](docs/superpowers/plans/2026-09-15-rekordbox-stage-c.md). Перед изменением базы приложение заново получает состав источника и показывает целевой плейлист, порядок, пути и изменения. Неполный ответ сервиса, отсутствующий файл, неоднозначное совпадение или изменившийся после просмотра план останавливают применение. Rekordbox должен быть закрыт.

WAV-копии подготавливаются отдельно; исходные файлы сохраняются. Перед сменой пути проверяются частота, каналы, число отсчётов и декодированное содержимое аудио. Существующие идентификаторы треков, cue-точки, сетка и пользовательские метаданные сохраняются. Путь общий для всех плейлистов Rekordbox, содержащих этот трек, поэтому просмотр изменений показывает затронутые связи. Возврат к оригиналам использует тот же просмотр и подтверждение.

Инженерные проверки на созданных тестовых базах не заменяют приёмку в настоящем Rekordbox. Полноценный публичный релиз также требует проверки установщика, подписи, обновления и реальных аккаунтов.

Сборка и проверки: [BUILD.md](BUILD.md). Исторический [PLAN.md](PLAN.md) сохранён для контекста; новые решения имеют приоритет.
