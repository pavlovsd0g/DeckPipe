{
  "name": "deckpipe-desktop",
  "private": true,
  "version": "0.5.0",
  "scripts": {
    "tauri": "tauri",
    "build": "npm --prefix .. run build:frontend && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
