# DeckPipe: уточнённый план релиза Windows

Решения пользователя от 9 сентября 2026 года имеют приоритет над первоначальным аудитом и старым PLAN.md. Этапы A и B выполняются сейчас; C и D следуют после их проверки.

## 1. Объём продукта

**Только Windows x64.** macOS, её сборка, подпись и тестирование исключены из текущего выпуска.

**SoundCloud:** обязательны сохранение музыки, чтение источников/лайков и синхронизация с локальными файлами через существующее решение. Создание плейлистов в сервисе и равенство функций с Deezer не обязательны. Сохраняются текущие загрузчики, без обязательного перехода на официальный OAuth.

**Локальная музыка:** пользователь должен сначала указать единую общую папку. Внутри могут быть любые вложенные папки. У плейлиста есть отдельная привязка папки назначения; её название может отличаться от названия в стриминге. Явно подключённая внешняя папка также индексируется.

## 2. Новый обязательный сценарий: «Лайки» по всей библиотеке

Например, трек One лежит в `Музыка/Warmup`, Two — в `Музыка/Peak time`, а в «Лайках» стриминга находятся One, Two и Three. DeckPipe показывает первые два как найденные с реальными путями и скачивает только Three в выбранную папку назначения.

Для этого реализуется постоянный каталог: сервис и ID, исполнитель, полное название версии, длительность, формат, пути, состояние доступности и признаки изменения файла. Один трек может участвовать в нескольких плейлистах без создания дополнительных копий. Плейлист стриминга и физическое размещение файла — разные сущности.

Неоднозначные совпадения требуют выбора файла. Исчезнувший диск не считается пустой библиотекой. Изменённый или заменённый файл повторно проверяется. Существующая ошибка конвертации остаётся отдельной задачей; синхронизация не скрывает её как успешную загрузку.

## 3. Этап A — целостность локальной библиотеки

- Точное сопоставление исполнителя и полной версии трека, защита от присвоения чужого файла.
- Публикация загрузок и WAV без перезаписи существующих файлов, раздельные имена при коллизиях.
- Восстановление JSON из резервной копии и журнал переименования, переживающий аварийное завершение процесса.
- Глобальный каталог, обязательная общая папка, фактические пути в плейлисте, ручное подтверждение совпадения.
- Загрузка только отсутствующих файлов, повторная проверка непосредственно перед обращением к сервису.

Критерий завершения: регрессионные сценарии проходят на реальных временных аудиофайлах; повторный синк не создаёт копий; отключение носителя останавливает очередь; сбои публикации и переименования сохраняют восстанавливаемое состояние.

## 4. Этап B — сервисы, вход и понятные результаты

- Полная пагинация коллекций, защита от повторяющихся курсоров и сторонних URL, явные ошибки вместо пустых «успешных» списков.
- Сохранение исходного провайдера при повторе ошибки, включая SoundCloud-трек в локальном плейлисте.
- Добавление в Deezer учитывается отдельно от загрузки. Ошибка хранится после перезапуска; повтор сначала проверяет текущий состав плейлиста.
- Общий жизненный цикл входа: открытие обычного браузера, ожидание, проверка, подключение, отмена, истечение времени, выход. Секреты не возвращаются в интерфейс.
- Для текущего браузера подготовлен Firefox-помощник Windows с нативным каналом и DPAPI. Пакеты Chrome/Edge потребуют отдельных ID и проверки.

Код и проверка на синтетических данных отделены от приёмки настоящего браузера. **Этап B нельзя считать полностью принятым, пока не проверены установка помощника, разрешения, реальный вход, перезапуск и загрузка в обоих сервисах.** Постоянная установка обычного Firefox требует подписанного Mozilla XPI; в лаборатории подготовлен неподписанный пакет.

## 5. Этап C — Rekordbox

Следующий этап: использовать фактические пути из глобального каталога при составлении плейлиста Rekordbox, включая «Лайки», распределённые по папкам. Проверить dry-run, отсутствие изменений при повторном применении, сохранность ContentID/меток/сетки, резервное копирование, транзакции и восстановление после сбоя. Для WAV отдельно проверить длительность и временные позиции. До этого массовая запись и WAV-переключение базы остаются закрыты.

## 6. Этап D — выпуск Windows

Чистая установка, обновление и удаление; Windows с кириллицей в имени пользователя; переносные/внешние диски; крупная библиотека; проверенные версии Rekordbox; подписи и происхождение артефактов; инструкция восстановления; установленный EXE и настоящий сценарий пользователя.

Публичный релиз допускается после этих проверок. Инженерная сборка, unsigned private-beta и публичный релиз имеют разные условия; успешная сборка сама по себе не меняет канал распространения.

## 7. Текущая реализация и доказательства

Исходники находятся в ветке `codex/release-hardening`. Подробные проверки и границы фактической приёмки фиксируются в отчёте реализации A/B рядом с этим планом. Первоначальная оценка проекта в аудите относится к состоянию до изменений и не переносится автоматически на новую сборку.
