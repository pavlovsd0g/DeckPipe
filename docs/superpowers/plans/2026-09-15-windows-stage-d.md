# Stage D: installed Windows and live collection acceptance

User instruction, 15 September 2026: "приступай, можешь брать пк под управление. Рекордбокс предлагаю не клонировать копией а проверять на моей". This authorizes Stage D and testing the actual Rekordbox collection. The earlier synthetic-only Stage C boundary is superseded for this stage. A verified rollback backup is retained; the test target is the actual collection, not a cloned test database.

## Current baseline and constraints

**Completed, 16 September, for unsigned private beta in the current owner environment.** `34a75a5` is installed. Real Google/SoundCloud login, both providers' download/reuse flows and persistent sessions after NSIS uninstall/reinstall passed. Actual Rekordbox apply/no-op/WAV/original/strict rollback and native playlist readback/cleanup passed. Internal dialog overflow is fixed. MSI was installed by the owner into `E:/DeckPipe`; exact payload, registration and native launch passed. Physical USB unplug/replug passed 38 checks. The owner explicitly excluded a different Windows user profile. All nine API latency/size budgets and native window/port/memory budgets passed. Settled foreground CPU passed the unchanged 100 ms/2 s budget with the computer-control observer disconnected. The historical entries below are not current acceptance claims; the [current outcome report](../../release-stage-d-2026-09-15.md) contains authoritative candidate-bound evidence and limits.

- Worktree `D:/Claude Code/Projects/deezer-rekordbox-sync/.worktrees/release-hardening`, branch `codex/release-hardening`, entry HEAD `d30a5f65e9b9df809a63de9130183fc602b9b904`. Stage C product/test/build evidence is bound to `6b3d8ab26a1b48edb6b22a07db4f8c8f5b3ba1a5`; the difference is documentation only.
- Preserve main's existing Orchestrator edit, SHA256 `15ca74f28c1c808798208d2452f2f7d9373a839f5080ee613e3cf44464cead6c`. No merge/push or publication is authorized.
- At entry, installed DeckPipe0.6.0 was the 28 August build under `D:/DeckPipe-RC-Lab/install/DeckPipe-0.6.0-private-beta`. It has now been upgraded by the pinned NSIS candidate from `8476b433b830cdab1496a961bc163992a3659001`; installed main/backend payload readback passed. Rekordbox7.2.14.0323 is installed on F:. Neither process remains running at this checkpoint. The current user's actual master.db and masterPlaylists6.xml are under APPDATA/Pioneer/rekordbox.
- On 16 September the owner excluded testing another Windows profile: use the current profile for all further acceptance. A Windows sign-out/sign-in cycle was not performed; app restart and NSIS uninstall/reinstall persistence were verified. Passwords and provider credentials are not exported, read or logged.
- Keep labs, generated media, candidate outputs, backups and redacted evidence under `D:/DeckPipe-RC-Lab`. The pre-existing private-beta signing waiver remains an explicit channel constraint, never a signed/public PASS. Preserve old candidate pin/evidence; a new candidate gets its own stage and pin.
- Use the current Stage C exact-preview and separate recovery contracts. Old handoff references to an environment-only experiment flag or browser extension are historical and must not be reinstated.
- Windows UI uses the provided Computer Use API. Do not substitute raw PowerShell UI Automation for a failed tool. Provider authentication, MFA and security prompts remain owner actions.

## Execution checklist — initial checkpoint history

1. **PASS — Candidate and host preflight.** Fresh private-beta candidate `8476b43` built offline and release verifier PASS with signing/timestamp WAIVED_BY_OWNER. Seven staged files are pinned in `candidate-pin-8476b43.json`; MSI payload/provenance checks 23/23. Actual collection backup `D:/DeckPipe-RC-Lab/backups/rekordbox/stage-d-20260915-baseline-2` passed integrity, full logical comparison and 3,184 external-file hash checks. Main's user edit remains intact. Preserve the older candidates and the first failed preflight as historical evidence.
2. **PARTIAL — Installed lifecycle.** NSIS upgrade exit0; main matches the pinned candidate after the exact Tauri NSS marker transform, backend matches MSI bytes. Existing settings and encrypted credential-store bytes are unchanged. Installed backend checks 32/32 in isolated profile, including restart and real WAV/PCM readback. Native GUI, login/session validity, clean-profile first run, Windows logout/login, uninstall/reinstall and actual MSI installation remain PENDING; backend checks do not close those gates.
3. **PARTIAL — Live providers.** Deezer passed owner login, persistence through upgrade/process restart, 39 playlists, all 3,655 Favorites, reuse of95 files from13 folders and one bounded download plus repeat without download. SoundCloud live sync returned provider_access_denied; cached17 sources are not acceptance. Owner Google login exposed the popup denial, now corrected and verified with a synthetic native cross-origin handoff. Real Google/SoundCloud login and download remain pending.
4. **PARTIAL — Actual Rekordbox collection.** Read-only checks on the owner's actual database passed: 1,061 tracks, 17 playlists, 692 memberships, 1,091 cues; supported recovery schema. Preview of three existing tracks from three folders passed without writes. 1,055 files are available; six stored paths are missing, with one exact-filename candidate in configured root `E:/Music` (identity not confirmed; no relinking). Actual apply/no-op/WAV/original return, native Rekordbox readback and rollback verification remain PENDING. Regenerate the baseline if anything changed and always create a fresh preview immediately before an authorized write. Never overwrite an unrelated concurrent edit.
5. **PARTIAL — Scale and media availability.** Installed backend passed 66 checks against 5,001 generated files in an isolated lab profile: cold scan16.922s, warm scan1.859s, reuse of51 files from51 folders1.890s, restart, explicit external folder and unavailable/restored common root. These are measured observations, not five-sample native GUI performance-budget acceptance. Real removable-device and native UI checks remain PENDING. No owner drive was disconnected and no original music was moved or deleted.
6. **PENDING — Closure.** Fix actual findings with focused RED/GREEN plus affected verification and final candidate rebuild when product changes. Record installed/live outcomes, candidate hashes, open owner interactions and the precise release channel. Documentation and source tests alone cannot close D.

## Initial tool issue

The Windows `node_repl` tool initially failed before executing initialization with `failed to write kernel assets: ... os error3`. This historical issue is resolved; native DeckPipe and provider windows were controlled successfully. No request to restart Codex remains pending.

Evidence roots: `D:/DeckPipe-RC-Lab/qa-evidence/windows-stage-d-20260915` and `windows-stage-d-20260916`. Private-beta acceptance is complete; a signed public release is not established.

## Resume checkpoint — initial history

- Installed candidate and all seven pinned files were rehashed after testing. User settings/encrypted-store hashes and actual master.db are unchanged; no DeckPipe/backend/Rekordbox processes remain. See `checkpoint-verification.json` and [the progress report](../../release-stage-d-2026-09-15.md).
- The first backup check detected newly created empty WAL/SHM files. A separate read-only diagnostic reproduced SQLite's sidecar creation while preserving main.db bytes. The second actual-collection preflight retained the strict hash check and passed; its logical fingerprint also matches the first preflight. No playlist/path mutation or restore was requested.
- GUI control has resumed. Continue the popup candidate rebuild and actual Google/SoundCloud login, then actual Rekordbox apply/readback/rollback. Provider credentials and UAC remain owner interactions.
- `ef2e0c6` contains the native findings' frontend/auth corrections and passed Python380/1skip, PowerShell62 and Rust19 checks. Its installer and all seven pinned files remain immutable. Further popup changes require a new revision and candidate. No merge, push, publication or public-release PASS is implied.

## Final 16 September checkpoint

- Product source and installed NSIS payload: `34a75a5909bdb7c82bfe1ac0bf49d2c3db85312e`. Later documentation commits do not change that binary's provenance. Source gate: Python382/1skip, PowerShell62, Rust19; MSI readback23/23. Preserve the candidate and source-gate files under `windows-stage-d-20260915`.
- Owner Google authentication is complete; do not ask them to log in again. Installed SoundCloud account read returned16 playlists with no errors, verified audio download and missing-only repeat passed. Fresh post-reinstall read validated both provider sessions, all3655 Deezer Favorites and95 existing files across13 folders.
- Actual Rekordbox baseline3 has1095 cues. Native verification playlist was removed through Rekordbox; all original content/playlist/membership/cue rows and source audio hashes match the baseline. Native Rekordbox legitimately updated other DB state and masterPlaylists6.xml; never restore the older whole database over those changes. The journal's complete entry is historical, not an interrupted operation.
- NSIS uninstall/reinstall passed;888 backed-up file hashes preserved/restored. Test bindings and the owned media-index entry have been removed, leaving the common catalog at1268 files. Evidence audio remains in the lab.
- MSI product `{C5E9DF69-364B-447A-87C6-1791B4F1E1C6}` is installed per-machine in `E:/DeckPipe`. Owner installation, exact payload hashes and native UI readback passed. The first background attempt ended with 1602 before installation; no UAC request remains pending. NSIS remains under its original private-beta path. MSI uninstall was not separately exercised; do not infer it from the passed NSIS cycle. Preserve the old August uninstaller already present in the owner's E: directory.
- New evidence: `D:/DeckPipe-RC-Lab/qa-evidence/windows-stage-d-20260916`. Five native observations show2.14–2.68s to window,2.00–2.09s to port,497–519MB working memory; CPU93.8–156.2ms/2s fails100ms in4/5. Initial screenshots can still be white, so content readiness is not inferred. API five-sample warm medians passed all nine route and response-size budgets.
- Physical USB `G:` / `DJ`: existing file reuse, actual unplug, offline catalog retention and download rejection, return with unchanged file identity/hash, and owned fixture cleanup passed. Evidence: `windows-stage-d-20260916/usb-34a75a5/result.json`. The Windows user stayed unchanged; only application data was isolated.
- CPU diagnosis is closed: the same MSI PID and ten-process tree produced 0–15.6 ms/2 s with the Sky session disconnected, 140.6–156.2 ms with it connected, then 0 ms after disconnection. All 300 foreground checks and measured interval lengths were valid. Detached 10/10 samples passed the unchanged budget; these are settled samples from one launch, not five new cold starts. Independent review confirmed the interpretation. The exact observer mechanism is unknown. Historical FAIL runs remain preserved. See `native-cpu-observer-acceptance.json`.
- The raw-input-filter debug probe did not establish a fix and was not promoted to product code. Seven pinned artifacts, both installed payloads and the user's Orchestrator edit rehashed correctly. Of 889 pre-MSI baseline files, only the rescanned catalog changed; settings, encrypted credentials, auth browser profiles and actual Rekordbox DB remain intact. See `final-candidate-checkpoint.json`.
- Stage D is complete within the current unsigned private-beta scope. Windows sign-out/sign-in and a separate MSI uninstall remain unverified; the separate Windows profile was excluded by the owner. Public signing/timestamp and broader Windows acceptance remain release conditions. No merge/push/publication or signing waiver extension is authorized.
